<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mb-4"> Basic Blog</h1>
        <a href="/articles/new" class="btn btn-success mb-4">Create New Article</a>

        <% articles.forEach((article) => { %>
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title"><%= article.title %></h5>
                    <p class="card-subtitle text-muted mb-2"><%= new Date(article.createdAt).toLocaleDateString() %></p>
                    <p class="card-text"><%= article.description %></p>
                    <a href="/articles/<%= article.id %>" class="btn btn-primary">Read More</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>